import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class Oro here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class Oro extends Actor
{
    public Oro(){
        getImage().scale(getImage().getWidth()/10,getImage().getHeight()/10);
    }
    //en la entrega final se utulizara esta parte y se le agregara monedas tambien 

    /**
     * Act - do whatever the Oro wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public void act()
    {
        
        // Add your action code here.
    }
}
